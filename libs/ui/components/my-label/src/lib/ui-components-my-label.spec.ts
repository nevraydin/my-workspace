import { uiComponentsMyLabel } from './ui-components-my-label';

describe('uiComponentsMyLabel', () => {
  it('should work', () => {
    expect(uiComponentsMyLabel()).toEqual('ui-components-my-label');
  });
});
